import styled from "styled-components";

export const LineSeparatorStyled = styled.span`
  display: block;
  padding: 0.0625rem;
  margin: 1.5rem 0;
  background-color: var(--c-gray-300);
  border: none;
`;
